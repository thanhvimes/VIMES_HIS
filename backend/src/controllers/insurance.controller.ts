
export class InsuranceController {
  async getDocuments(req: any, res: any) { res.json([]); }
  async sendDocuments(req: any, res: any) { res.json([]); }
  async signDocuments(req: any, res: any) { res.json({ success: true }); }
}
