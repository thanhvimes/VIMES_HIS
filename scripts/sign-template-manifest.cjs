const fs=require('node:fs'),crypto=require('node:crypto');const [mode,file,signature]=process.argv.slice(2);const secret=process.env.TEMPLATE_PACKAGE_SIGNING_SECRET;if(!secret||!file||!['sign','verify'].includes(mode)){console.error('Set TEMPLATE_PACKAGE_SIGNING_SECRET; usage sign|verify manifest.json [sig]');process.exit(2)}const value=crypto.createHmac('sha256',secret).update(fs.readFileSync(file)).digest('hex');if(mode==='sign'){fs.writeFileSync(signature||`${file}.sig`,value);console.log(value)}else{const expected=fs.readFileSync(signature||`${file}.sig`,'utf8').trim();console.log(JSON.stringify({valid:value===expected}));if(value!==expected)process.exitCode=1}
