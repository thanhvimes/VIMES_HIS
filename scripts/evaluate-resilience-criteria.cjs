const fs=require('node:fs');const file=process.argv[2];if(!file){console.error('Usage: evaluate-resilience-criteria.cjs result.json');process.exit(2)}const d=JSON.parse(fs.readFileSync(file));const checks={noLostJobs:Number(d.lostJobs||0)===0,noDuplicateArtifacts:Number(d.duplicateArtifacts||0)===0,p95UnderSla:Number(d.p95Ms||0)<=Number(process.env.BENCHMARK_P95_MS||3000),queueStable:Number(d.queueGrowth||0)<=0,ramStable:Number(d.ramGrowthPercent||0)<=Number(process.env.BENCHMARK_RAM_GROWTH_PERCENT||25)};const out={checks,passed:Object.values(checks).every(Boolean),source:file};console.log(JSON.stringify(out));if(!out.passed)process.exitCode=1;
