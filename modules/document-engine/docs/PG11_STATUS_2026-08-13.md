# PG-11 status — 2026-08-13

## Đã đạt trong staging

- PAdES-B-T signing bằng test PKCS#12 certificate.
- Independent validation: signed PDF `VALID`, tampered PDF `INVALID`.
- Readiness fail-closed: provider chưa hỗ trợ, thiếu PFX, certificate hết hạn.
- Input/output size guard và base64 validation.
- `/metrics`, `/metrics/prometheus`, certificate expiry metric.
- Prometheus scrape config, alert rules và monitoring README.
- Validator script, contract tests **12/12**, backend/frontend typecheck.
- Intake form, evidence manifest và production execution runbook.
- Provider contract fail-closed test: digest signing chưa có adapter thì trả `NotImplementedError`; contract tests **13/13**.
- TSA factory test đã xác nhận lỗi timeout được propagate/fail-closed; contract tests **14/14**.
- Đã tạo `services/pdf-signing/scripts/load_test.py` đo throughput và p50/p95/p99 latency bằng synthetic PDF; script compile đạt, runtime load test chờ SLA/provider production.
- Thêm `X-Request-ID` middleware để tương quan audit giữa HIS và signing service; contract tests **15/15**.
- Thêm concurrency guard cấu hình qua `SIGNING_MAX_CONCURRENCY` (mặc định 4), quá tải trả HTTP 429; compile và regression tests đạt.
- Baseline staging 10 request/concurrency 2: 10/10 thành công, throughput **5.52 RPS**, p50 **319 ms**, p95 **417 ms** trên synthetic PDF; chưa phải SLA production.
- Staging 50 request/concurrency 4: 50/50 thành công, throughput **6.47 RPS**, p50 **593 ms**, p95 **784 ms**, p99 **830 ms**; chưa phải SLA production.
- Soak staging 100 request/concurrency 4: 100/100 thành công, throughput **6.60 RPS**, p50 **588 ms**, p95 **751 ms**, p99 **871 ms**, errors **0**; chưa phải SLA production.
- Spike staging image mới 40 request/concurrency 20: **8 thành công, 32 bị từ chối do concurrency guard (HTTP 429)**, không treo service; throughput thành công **4.77 RPS**, p50 **850 ms**, p95 **930 ms**.
- Load tool đã phân loại riêng `rejected_429`; chạy xác nhận 20 request/concurrency 10: 7 thành công, 13 rejected_429, 0 lỗi hệ thống.
- Audit hardening: `/v1/complete` chỉ lưu SHA-256 và độ dài CMS, không lưu raw CMS payload; contract tests **16/16**.
- Runtime log scan không phát hiện private key, PFX, PIN hoặc raw CMS payload.
- Container security scan đạt: `ReadonlyRootfs=true`, `Privileged=false`, secret mount `/run/secrets` read-only.
- Network hardening: Compose mặc định bind `127.0.0.1`, chỉ mở signing port nội bộ; production không expose public nếu chưa có reverse proxy/network policy.
- Regression sau hardening đạt **16/16 contract tests**.
- Bổ sung `X-Idempotency-Key` cho `/v1/sign-pdf` để client retry không tạo ký trùng; contract tests **17/17**.
- Idempotency staging có TTL 900 giây qua `SIGNING_IDEMPOTENCY_TTL_SECONDS`; production vẫn bắt buộc shared Redis/PostgreSQL.
- Giới hạn `X-Idempotency-Key` tối đa 128 ký tự để chống lạm dụng cache; contract tests **18/18**.
- Prometheus bổ sung `vimes_sign_rejected_concurrency_total` để theo dõi backpressure HTTP 429; contract tests **19/19**.
- Monitoring README đã bổ sung hướng xử lý khi concurrency rejection tăng: kiểm tra provider latency/queue, scale worker trong giới hạn và chạy lại synthetic load test.
- Prometheus bổ sung `vimes_signing_max_concurrency` để dashboard hiển thị capacity cấu hình; contract tests vẫn đạt **19/19**.
- Đã tạo `services/pdf-signing/monitoring/CAPACITY_PLANNING.md` với baseline staging và quy trình scale production.
- Đã tạo ma trận kiểm thử timeout/5xx/TSA/OCSP/failover/certificate tại `PROVIDER_FAILURE_MATRIX.md`; runtime execution chờ provider thật.
- Log scan tự động `scan-sensitive-logs.ps1` chạy đạt trên container staging, không phát hiện secret/PHI pattern.
- Container security script `check-container-security.ps1` chạy đạt: read-only rootfs, non-privileged, secrets read-only.
- CI đã thêm security gate chạy `check-container-security.ps1` và `scan-sensitive-logs.ps1` trên signing image.
- Hai PowerShell security script đã được parse-check thành công và có bước kiểm tra tương ứng trong CI.
- Kiểm tra tổng hợp cuối: PowerShell scripts parse, Python compile và `git diff --check` đều đạt; chỉ còn cảnh báo line-ending Windows.
- Runtime smoke sau recreate image mới: Prometheus metrics có capacity/concurrency/certificate expiry; container security và sensitive-log scan đều đạt.
- Baseline image mới 20 request/concurrency 2: 20 thành công, `rejected_429=0`, errors 0, throughput **4.34 RPS**, p50 **425 ms**, p95/p99 **556 ms**.
- Spike image mới 20 request/concurrency 10: 6 thành công, 14 `rejected_429`, 0 lỗi hệ thống; metrics ghi nhận `vimes_sign_rejected_concurrency_total=14`, capacity=4.
- Checklist đã đánh dấu load/soak/spike staging hoàn tất; load SLA production vẫn chờ provider CA thật và ngưỡng bệnh viện phê duyệt.
- Idempotency retry đã có contract test staging; runbook bổ sung yêu cầu kiểm chứng không ký trùng khi failover production.
- Provider failure matrix đã bổ sung kịch bản client retry sau timeout với cùng idempotency key.
- CI path filter đã bao gồm toàn bộ `services/pdf-signing/monitoring/**`, bảo đảm thay đổi rules/runbook kích hoạt release checks.
- CI-related local checks đạt: PowerShell parse và `git diff --check`; chỉ còn cảnh báo line-ending Windows.
- Tổng hợp checklist hiện tại: **19/41 mục đã đánh dấu staging (46,3%)**, còn 22 mục production phụ thuộc provider/CA, hạ tầng và phê duyệt liên phòng ban.
- Tạo `validate-production-config.ps1` và thêm parse gate CI: chặn provider test, bind public và thiếu TSA/OCSP/CRL.
- Production config gate đã test: cấu hình Remote CA + endpoint placeholder hợp lệ được pass; provider `test` bị chặn đúng kỳ vọng.
- Checklist/runbook đã liên kết production config gate như bước bắt buộc trước startup.
- Tiến độ checklist hiện tại: **23/45 mục đã đánh dấu hoàn thành (51,1%)**, còn 22 mục production cần provider/CA và phê duyệt.
- Kiểm tra tổng hợp cuối phiên: PowerShell parse, Python compile và diff check đều đạt; workspace sẵn sàng nhận thông tin provider production.
- Regression mới nhất: signing contract tests **19/19**, backend typecheck đạt, frontend typecheck đạt.
- Request ID audit đã có cả hai trường hợp client gửi và service tự sinh; contract tests **20/20**.
- Release security gate sau build mới đạt: container hardening pass và sensitive-log scan sạch.
- Mẫu `.env.production.example` đã bổ sung concurrency limit và idempotency TTL để cấu hình capacity/backpressure ngay từ production deployment.
- Production config gate chạy lại với cấu hình capacity/idempotency mới: **pass**.
- Checklist đã ghi nhận production env mẫu và config gate; runtime provider thật vẫn là blocker.
- Tiến độ checklist mới nhất: **21/43 mục (48,8%)**, còn 22 mục production cần provider/CA, hạ tầng và sign-off.
- Đã đặc tả shared idempotency production (atomic create, fingerprint conflict 409, lease/reclaim, retention); implementation runtime chờ Redis/PostgreSQL được cấp.
- Đã tạo migration `backend/migrations/053_hms_document_signature_idempotency.sql` và SQL runbook; sử dụng đúng prefix `hms_document_`, chỉ lưu hash/metadata.
- Migration safety test đạt: xác nhận prefix, unique idempotency key và không có PDF/CMS/private key/PIN field.
- Migration test đồng thời xác nhận expiry/lease index phục vụ cleanup và reclaim an toàn.
- Production config gate negative test đạt: chặn provider test, bind `0.0.0.0` và thiếu TSA.
- CI đã thêm bước chạy tự động `test-production-config-gate.ps1` cho mọi thay đổi signing service.
- Tiến độ checklist mới nhất: **23/45 mục (51,1%)**; đã đóng thêm release safety gate, còn 22 mục cần provider/CA và phê duyệt production.
- Blocker production đã chuẩn hóa tại `PG11_PRODUCTION_BLOCKER_TRACKER.md` với owner, đầu vào và tiêu chí đóng từng mục.
- Đã tạo `PG11_EVIDENCE_INDEX.md` làm điểm truy cập duy nhất tới toàn bộ evidence, runbook, monitoring và security gate.
- Load/soak/spike evidence, provider failure matrix và capacity planning đã được cập nhật; provider production thật vẫn là blocker.

## Chưa đạt production

- Provider thật: HSM/PKCS#11, USB Token/Local Agent hoặc Remote CA.
- Trust chain CA, TSA/OCSP/CRL thật và kiểm thử outage/rotation.
- Vault/KMS, mTLS/network policy, failover/circuit breaker.
- Load/spike/soak test theo SLA bệnh viện.
- Clinical/Security/Infrastructure/Legal sign-off, rollback drill và GO/NO-GO.

## Điểm bắt đầu phiên tiếp theo

1. Điền `SIGNING_PROVIDER_INTAKE_FORM.md` với provider được phê duyệt.
2. Chạy `PRODUCTION_PROVIDER_EXECUTION_RUNBOOK.md` theo môi trường staging gần production.
3. Lưu kết quả vào `PG11_EVIDENCE_MANIFEST_TEMPLATE.md`.

Trạng thái tổng thể: **STAGING READY / PRODUCTION NOT READY**.

- Đã tạo backend adapter `vimes-signing.service.ts` để HIS gọi Signing Server mới, có request ID, idempotency, timeout và mapping lỗi; backend typecheck đạt.
- Thêm route test HIS có auth `POST /api/health-check/documents/sign-pdf-vimes`, dùng adapter mới để test PDF/PAdES mà không ảnh hưởng luồng HMS/XML cũ.
- Adapter HIS integration test đã xác nhận payload chuẩn, endpoint `/v1/sign-pdf`, request ID và idempotency headers; backend typecheck đạt.
- Đã tạo tài liệu `HIS_VIMES_SIGNING_INTEGRATION.md` gồm cấu hình, request mẫu và mapping lỗi HIS/Signing Server.
- Đã tạo `backend/scripts/smoke-vimes-signing.ps1` để test một lệnh HIS → Signing Server bằng PDF synthetic; script parse đạt.
